import React from 'react';
import { getUserInfor } from 'services/teleBot';

type Props = {};

const Home: React.FC<Props> = () => {
    // getUserInfor()

    return (
        <div>
            HOME
        </div>
    );
};

export default Home;